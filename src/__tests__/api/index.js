import React from 'react';
import { shallow, mount, render } from 'enzyme'; // render
import { createPostObject } from  '../../api/index';

it('renders the createPostObject function correctly', () => {
   /*var author = 'Zac';
   var title= 'My title';
   var content='My content';
   var id="3";
   var date= (new Date()).toLocaleString();
   console.log(createPostObject);
   var result = createPostObject( title, content,   author)
   var expected = {
     title,
     content,

     author,

     date:(new Date()).toLocaleString()

    }
  expect(result).toBe(expected) */
});
